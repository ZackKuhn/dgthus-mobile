/*define as tipagens para os nomes das 
páginas disponíveis para navegação*/
export type RootStackParamList = {
  IndexPage: { id: string };
  LoginPage: { id: string };
  ForgotPage: { id: string };
  SchedulePage: { id: string};
}